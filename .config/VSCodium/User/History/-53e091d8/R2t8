# PUBLIC API variables: The IP address and port on which the endpoints within the React Native app listen to. 
# For local testing, use the 'roetz-mock-server'. 

# For Android, use 10.0.2.2 (special alias for emulator host loopback).
# For iOS, use localhost or 127.0.0.1.
# Source: https://developer.android.com/studio/run/emulator-networking
EXPO_PUBLIC_API_IP=10.0.2.2
EXPO_PUBLIC_API_PORT=3000

# DEV MODE toggle:
# Set to "true" to skip login authentication and go directly to the dashboard. 
# This is useful during development to quickly test the dashboard without logging in.
# Set to "false" to enable Firebase authentication, requiring valid credentials to log in or register.
EXPO_PUBLIC_DEV_MODE=true
